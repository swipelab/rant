name: app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.7
  provider: 6.0.1
  freezed_annotation: ^0.15.0
  stated: 1.0.8
#    git:
#      url: https://github.com/swipelab/stated.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.5
  freezed: ^0.15.1+1

flutter:
  uses-material-design: true

